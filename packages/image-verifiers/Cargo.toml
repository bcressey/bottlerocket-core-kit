[package]
name = "image-verifiers"
version = "0.1.0"
edition = "2021"
publish = false
build = "../build.rs"

[lib]
path = "../packages.rs"

[package.metadata.build-package]
source-groups = [
    "generate-readme",
    "image-verifiers",
]

[build-dependencies]
glibc = { path = "../glibc" }
